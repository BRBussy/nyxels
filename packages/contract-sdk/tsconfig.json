{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The dust-estimation helper uses the universal `fetch` (browser + Node 18+).
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src", "managed/contract/index.d.ts"]
}
